pipeline {
  agent any 
  stages {
    stage('Verify version') {
      steps {
        sh 'docker run -u root –rm -d –name renzjenkins -v $(which docker):/usr/bin/docker -v jenkins-data:/var/jenkins_home -v /var/run/docker.sock:/var/run/docker.sock flyway/flyway' 
      }
    }
  }
}