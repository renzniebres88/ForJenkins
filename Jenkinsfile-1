pipeline {
  agent any 
  stages {
    stage('Verify version') {
      steps {
        sh 'docker run --rm -v /var/run/docker.sock:/var/run/docker.sock flyway/flyway:8.5.1 version'
      }
    }
  }
}